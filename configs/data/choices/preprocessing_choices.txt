data_cleaner:
 name: 'BaselineDataCleaner'

data_vectorizer:
    text_vectorizer:
        type: 'embedding', 'tfidf'
        name: 'EmbeddingTextVectorizer', 'TfIdfTextVectorizer'
        embeddings:
            embedding_type: 'glove', 'bpe', 'word2vec'
            embedding_dim: 25, 50, 100
            text_encoder:
                name: 'TextEncoder'
                max_vocab_size: ANY
                max_seq_len: ANY

    output_vectorizer:
        name: BasicOutputVectorizer